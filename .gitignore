# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/

# PyInstaller
build/
dist/
*.manifest
*.exe

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Node (in case any tooling is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Local config / secrets
# The real Firebase web config lives here in deployments but is kept out
# of the repo. It's public-by-design (ships to every browser; data is
# secured by firestore.rules), but we don't commit it — see
# pm5web/firebase-config.example.js for the template.
pm5web/firebase-config.js
# Concept2 PM5 BLE spec — third-party reference, kept locally, not published.
pm5_ble_spec.pdf
.env
.env.local
.env.*.local
*.local

# Logs / temp
*.log
.tmp/
tmp/

# Auto-generated icons (keep sources, ignore re-generations)
pm5web/icon-*.png.bak
